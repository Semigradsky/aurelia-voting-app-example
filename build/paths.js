const appRoot = 'src/';
const outputRoot = 'dist/';

export default {
  root: appRoot,
  source: appRoot + '**/*.js',
  html: appRoot + '**/*.html',
  style: appRoot + '**/*.css',
  output: outputRoot
};
